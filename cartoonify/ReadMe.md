# Cartoonify an Image with OpenCV in Python.

1. Build a python application that will transform an image into its cartoonified version using OpenCV.
2. Another filter transforms the image to a hand drawn look.

